language: php

php:
  - 5.5
  - 5.6
  - hhvm
  - 7.0

matrix:
  allow_failures:
    - php: hhvm
    - php: 7.0

install:
  - composer install --no-interaction

script: vendor/bin/phpunit --verbose

notifications:
  irc: "irc.freenode.net#sitech"

sudo: false